<% for(var i =0; i < items.length; i++){ %>
  <tr <%= items[i]%> tabindex='0' class="<%= rowClassName %>">
  	<%== view(rowTemplate, items[i], {"columns": columns, "renderer": renderer, "count": count }) %>
  </tr>
<% } %>